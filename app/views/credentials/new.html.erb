<%= turbo_frame_tag 'new-credential' do%>
  <h1>New credential</h1>

  <%= render "form", credential: @credential %>

  <br>

  <div>
    <%= link_to "Back to credentials", credentials_path %>
  </div>
<% end %>
