<%= form_with(model: credential) do |form| %>
  <% if credential.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(credential.errors.count, "error") %> prohibited this credential from being saved:</h2>

      <ul>
        <% credential.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.text_field :name, placeholder: 'Name of the application' %>
  </div>

  <div class="mb-3">
    <%= form.text_field :login_id, placeholder: 'Login Id' %>
  </div>

  <div class="mb-3">
    <%= form.text_field :login_password, placeholder: 'Password' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
